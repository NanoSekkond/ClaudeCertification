{
    "name": "get_current_datetime",
    "description": "Returns the current date and time formatted according to the specified format string. Defaults to YYYY-MM-DD HH:MM:SS format if no format is provided.",
    "input_schema": {
        "type": "object",
        "properties": {
            "date_format": {
                "type": "string",
                "description": "strftime format string for datetime output (default: '%Y-%m-%d %H:%M:%S')",
                "default": "%Y-%m-%d %H:%M:%S"
            }
        },
        "required": []
    }
}